<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="XrayUI.Views.ProxyRuntimeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="WindowRoot" RowSpacing="10" Padding="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid ColumnDefinitions="Auto,*,Auto,Auto" ColumnSpacing="10">
            <TextBlock Text="查看" VerticalAlignment="Center" />
            <ComboBox x:Name="TargetSelector" Grid.Column="1" SelectionChanged="TargetSelector_SelectionChanged" />
            <Button x:Name="StopTargetButton" Grid.Column="2" Content="关闭当前代理"
                    Click="StopTargetButton_Click" />
            <Button Grid.Column="3" Content="复制" Click="CopyButton_Click" />
        </Grid>
        <TextBlock x:Name="StatusText" Grid.Row="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        <ScrollView x:Name="LogScrollViewer" Grid.Row="2" ContentOrientation="Both"
                    VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <TextBlock x:Name="LogText" FontFamily="Cascadia Code, Consolas, Courier New"
                       FontSize="12" TextWrapping="NoWrap" IsTextSelectionEnabled="True" />
        </ScrollView>
    </Grid>
</Window>
