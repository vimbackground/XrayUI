<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="XrayUI.Views.ServerListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="using:XrayUI.Models"
             xmlns:vm="using:XrayUI.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="420">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Vertical">
            <TextBlock Text="服务器列表"
                       FontSize="20"
                       FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        Spacing="8"
                        Margin="0,8,0,0">
                <DropDownButton x:Name="BtnAdd"
                                Padding="12,6">
                    <DropDownButton.Content>
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <TextBlock Text="添加" VerticalAlignment="Center" />
                        </StackPanel>
                    </DropDownButton.Content>
                    <DropDownButton.Flyout>
                        <MenuFlyout Placement="BottomEdgeAlignedLeft">
                            <MenuFlyoutItem Text="导入节点"
                                            Command="{x:Bind ViewModel.ImportFromLinkCommand}"
                                            FontWeight="Normal">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE77F;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem Text="添加订阅"
                                            Command="{x:Bind ViewModel.OpenSubscriptionsCommand}"
                                            FontWeight="Normal">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xED11;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem Text="手动添加"
                                            Command="{x:Bind ViewModel.AddManualCommand}"
                                            FontWeight="Normal">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE710;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
                <Button x:Name="BtnEdit"
                        Command="{x:Bind ViewModel.EditServerCommand}"
                        IsEnabled="{x:Bind ViewModel.CanEditSelectedServer, Mode=OneWay}"
                        Padding="12,6">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon Glyph="&#xE70F;"
                                  Margin="0,0,5,0" />
                        <TextBlock Text="编辑" />
                    </StackPanel>
                </Button>
                <Button x:Name="BtnRemove"
                        Command="{x:Bind ViewModel.RemoveServerCommand}"
                        IsEnabled="{x:Bind ViewModel.CanRemoveSelectedServer, Mode=OneWay}"
                        Padding="12,6">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon Glyph="&#xE74D;"
                                  Margin="0,0,5,0" />
                        <TextBlock Text="删除" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>
        <Grid Grid.Row="1"
              Margin="0,16,0,0"
              ColumnSpacing="8"
              HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <AutoSuggestBox x:Name="ServerSearchBox"
                            Grid.Column="0"
                            Width="350"
                            PlaceholderText="搜索服务器"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            UpdateTextOnSelect="False"
                            QueryIcon="Find"
                            TextChanged="ServerSearchBox_TextChanged"
                            SuggestionChosen="ServerSearchBox_SuggestionChosen"
                            QuerySubmitted="ServerSearchBox_QuerySubmitted">
                <AutoSuggestBox.ItemTemplate>
                    <DataTemplate x:DataType="models:ServerEntry">
                        <TextBlock Text="{x:Bind Name, Mode=OneWay}" />
                    </DataTemplate>
                </AutoSuggestBox.ItemTemplate>
            </AutoSuggestBox>

            <ToggleButton Grid.Column="1"
                          Background="Transparent"
                          BorderBrush="Transparent"
                          Padding="6"
                          IsChecked="{x:Bind ViewModel.IsFilterPanelOpen, Mode=TwoWay}"
                          Visibility="{x:Bind ViewModel.IsChipBarVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                          VerticalAlignment="Center"
                          AutomationProperties.Name="筛选">
                <FontIcon Glyph="&#xE8A9;"
                          FontSize="15" />
            </ToggleButton>
        </Grid>
        <Grid Grid.Row="2"
              Margin="0,12,0,0"
              ColumnSpacing="8"
              HorizontalAlignment="Left"
              Visibility="{x:Bind ViewModel.IsFilterBarVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0"
                        Orientation="Horizontal"
                        Spacing="6"
                        VerticalAlignment="Center">
                <FontIcon Glyph="&#xEA37;"
                          FontSize="12"
                          Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock Text="筛选条件:"
                           FontSize="14"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           VerticalAlignment="Center" />
            </StackPanel>

            <ComboBox Grid.Column="1"
                      ItemsSource="{x:Bind ViewModel.GroupChips, Mode=OneWay}"
                      SelectedItem="{x:Bind ViewModel.SelectedChip, Mode=TwoWay}">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="vm:ServerGroupChip">
                        <TextBlock Text="{x:Bind DisplayName, Mode=OneWay}"
                                   TextTrimming="CharacterEllipsis" />
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <DropDownButton Grid.Column="2"
                            Background="Transparent"
                            BorderBrush="Transparent"
                            AutomationProperties.Name="排序">
                <DropDownButton.Content>
                    <FontIcon Glyph="&#xE8CB;" FontSize="15" />
                </DropDownButton.Content>
                <DropDownButton.Flyout>
                    <MenuFlyout Placement="Bottom">
                        <RadioMenuFlyoutItem Text="默认"
                                             GroupName="ServerSortMode"
                                             IsChecked="{x:Bind ViewModel.IsSortDefault, Mode=TwoWay}" />
                        <RadioMenuFlyoutItem Text="当前连接"
                                             GroupName="ServerSortMode"
                                             IsChecked="{x:Bind ViewModel.IsSortActive, Mode=TwoWay}"
                                             IsEnabled="{x:Bind ViewModel.CanSortByActive, Mode=OneWay}"
                                             ToolTipService.ToolTip="只在筛选为「所有服务器」时可用" />
                        <RadioMenuFlyoutItem Text="协议"
                                             GroupName="ServerSortMode"
                                             IsChecked="{x:Bind ViewModel.IsSortProtocol, Mode=TwoWay}" />
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>
        </Grid>

        <ListView Grid.Row="3"
                  x:Name="ServersListView"
                  Margin="0,8,0,0"
                  ItemsSource="{x:Bind ViewModel.VisibleServers, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.SelectedServer, Mode=TwoWay}"
                  CanDragItems="{x:Bind ViewModel.CanReorderInCurrentChip, Mode=OneWay}"
                  CanReorderItems="{x:Bind ViewModel.CanReorderInCurrentChip, Mode=OneWay}"
                  AllowDrop="{x:Bind ViewModel.CanReorderInCurrentChip, Mode=OneWay}"
                  DragItemsCompleted="ServersListView_DragItemsCompleted"
                  SelectionMode="Single"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:ServerEntry">
                    <Grid Background="Transparent"
                          ContextRequested="ServerItem_ContextRequested"
                          DoubleTapped="ServerItem_DoubleTapped">
                        <StackPanel Orientation="Vertical"
                                    Spacing="6"
                                    Margin="0,12,0,12">
                            <Grid VerticalAlignment="Center">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{x:Bind Name, Mode=OneWay}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           Grid.Column="0"
                                           TextTrimming="CharacterEllipsis" />

                                <Border Background="{ThemeResource SystemFillColorSuccessBrush}"
                                        CornerRadius="3"
                                        Padding="6,2"
                                        Visibility="{x:Bind IsActive, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                        Grid.Column="1"
                                        Margin="8,0,0,0">
                                    <TextBlock Text="● Activated"
                                               FontSize="12"
                                               FontWeight="SemiBold"
                                               Foreground="White" />
                                </Border>
                            </Grid>

                            <StackPanel Orientation="Horizontal"
                                        Spacing="8">
                                <TextBlock Text="{x:Bind Host, Mode=OneWay}"
                                           FontSize="13"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Text="{x:Bind Port, Mode=OneWay}"
                                           FontSize="13"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Text="{x:Bind DisplayProtocol, Mode=OneWay}"
                                           FontSize="13"
                                           FontWeight="SemiBold"
                                           Foreground="{x:Bind Protocol, Mode=OneWay, Converter={StaticResource ProtocolToBrushConverter}}" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
